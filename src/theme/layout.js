/**
 * Shared layout tokens for the public site.
 *
 * Containers cap at MUI's `xl` breakpoint (1536px) so wide monitors get a
 * wider reading column instead of a narrow strip in the middle; horizontal
 * padding and section spacing grow with the viewport as well.
 */
export const CONTAINER_PX = { xs: 3, md: 5, xl: 6 };
export const SECTION_PY = { xs: 10, md: 14, xl: 18 };
